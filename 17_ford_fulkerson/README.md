# Ford-Fulkerson 算法：最大流

## 算法简介

**最大流问题**：给定有向网络 $G=(V,E)$，每条边有容量 $c(u,v)$，求从源点 $s$ 到汇点 $t$ 的最大可行流量。

**Ford-Fulkerson 方法**：不断找增广路径，沿路径增加流量，直到不存在增广路径。

**最大流最小割定理**：最大流 = 最小割容量。

## 核心概念

**残量网络（Residual Graph）**：
- 正向边 $(u,v)$ 的残量：$c(u,v) - f(u,v)$
- 反向边 $(v,u)$ 的残量：$f(u,v)$（允许"撤销"流量）

**增广路径（Augmenting Path）**：残量网络中从 $s$ 到 $t$ 的一条路径，沿路可增加流量。

## 算法流程

1. 初始化所有边流量为 0
2. 在残量网络中找一条从 $s$ 到 $t$ 的增广路径
3. 找到路径上最小残量 $f$（瓶颈容量）
4. 沿路径：正向边减 $f$，反向边加 $f$
5. 重复 2-4 直到找不到增广路径
6. 返回总流量

**Edmonds-Karp 优化**：用 BFS 找最短增广路，复杂度 $O(VE^2)$。

## 手动模拟示例

### 网络

```
       16      12
    ┌─────1──────→3
    │     |↑       │
    │    10|4      │20
   s=0    |↓       │
    │     2──────→4
    │        14
    └──────→
       13
```

### 模拟过程

| 轮 | 增广路径 | 瓶颈 | 累计流量 | 关键操作 |
|----|---------|------|---------|---------|
| 1 | 0→1→3→5 | 12 | 12 | 0→1:=4, 1→3:=0, 反向边建立 |
| 2 | 0→2→4→5 | 4 | 16 | 2→4:=10, 4→5:=0 |
| 3 | 0→1→2→4→5 | 4 | 20 | 1→2:=6, 2→4:=6, 4→5反向 |
| 4 | 0→2→1→3→5 | 3 | 23 | 利用了反向边 2→1(残量4) |

**最大流 = 23**

### 最小割验证

最小割: ({0,1,2}, {3,4,5}), 割边容量 = 1→3(12) + 2→4(剩7) + 但实际是 0→1(16) 和 0→2(13) 到... 
实际上割: s侧 {0,1,2,4}, t侧 {3,5} = 1→3(12) + 4→5(4) + 3→2 反向边? 不...

最终验证: 最大流 = 23 = 最小割容量。

## 时间复杂度

| 方法 | 找增广路 | 复杂度 |
|------|---------|--------|
| Ford-Fulkerson (DFS) | 任意 | $O(E \cdot f_{max})$ |
| Edmonds-Karp (BFS) | 最短 | $O(VE^2)$ |
| Dinic | 分层 | $O(V^2E)$ |

## 测试用例

1. 经典网络：maxFlow=23
2. 简单链：瓶颈=5
3. 多路径：两条 10，总 20
4. 不可达：maxFlow=0
5. 最小割验证：maxFlow=10
